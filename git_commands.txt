git shortlog = number of commits authorwise
git log -2 = top 2 commits
git log -2 --oneline = 2 wale ka one line
git log -- ListDemo.html = finding commits where given files were part of that commit
git log -- CSS/mystyle.css TableDemo.html = we can do the same for multiple files
git log --pretty=format:"%an = %h : %ar = %s" = filter

git status = for current status

git branch featurebranch1 = to create new branch
git branch = lists all branches in working repository